#!/bin/bash
CURRENT=$(xrandr --query | grep " connected" | head -1 | awk '{print $1}')
if xrandr --query | grep "$CURRENT.*1920x1080" > /dev/null 2>&1; then
    xrandr --output "$CURRENT" --mode 1680x1050
else
    xrandr --output "$CURRENT" --mode 1920x1080
fi
